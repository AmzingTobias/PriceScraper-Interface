# The URL of the backend API server
# Set this in Vercel Environment Variables for production

# Used by Next.js rewrites (next.config.js) to proxy /api/* and /uploads/*
# to your backend, and by next/image remote patterns.
NEXT_PUBLIC_API_URL=http://localhost:5000
